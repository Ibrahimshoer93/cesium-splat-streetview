# Copy this file to `.env` and fill in your own Cesium Ion access token.
# Grab one at https://ion.cesium.com/tokens (the default token works for
# read-only public assets). Vite picks up VITE_-prefixed vars and exposes
# them via import.meta.env at build time.
VITE_CESIUM_ION_TOKEN=
